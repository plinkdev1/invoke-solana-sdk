<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto" >

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!--
        Android 11+ (API 30+) package visibility.
        Without this, PackageManager.queryIntentActivities() returns empty
        for wallet apps even when they are installed, so getInstalledWallets()
        returns [] and the MWA association intent cannot find a target.
    -->
    <queries>
        <intent>
            <action android:name="com.solana.mobilewalletadapter.walletlib.scenario.ACTION_HELLO"/>
        </intent>
        <package android:name="app.phantom"/>
        <package android:name="com.backpack.wallet"/>
        <package android:name="com.solflare.mobile"/>
    </queries>

    <application
        android:label="@string/godot_project_name_string"
        android:allowBackup="false"
        android:icon="@mipmap/icon"
        android:appCategory="game"
        android:isGame="true"
        android:hasFragileUserData="false"
        android:requestLegacyExternalStorage="false"
        tools:ignore="GoogleAppIndexingWarning" >

        <!--
            INVOKE MWA Plugin registration.
            MUST be inside <application> — Godot reads applicationInfo.metaData
            which only picks up meta-data that is a child of <application>.
            Placing this tag outside <application> (at manifest level) means
            Engine.has_singleton("InvokeMWA") always returns false.
        -->
        <meta-data
            android:name="org.godotengine.plugin.v2.InvokeMWA"
            android:value="com.invoke.mwa.MWAPlugin" />

        <!-- Records the version of the Godot editor used for building -->
        <meta-data
            android:name="org.godotengine.editor.version"
            android:value="${godotEditorVersion}" />

        <activity
            android:name=".GodotApp"
            android:label="@string/godot_project_name_string"
            android:theme="@style/GodotAppSplashTheme"
            android:launchMode="singleInstancePerTask"
            android:excludeFromRecents="false"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|density|keyboard|navigation|screenLayout|uiMode"
            android:resizeableActivity="false"
            tools:ignore="UnusedAttribute" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
